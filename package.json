{
  "name": "fh-maintenance-bot",
  "version": "2.0.0",
  "description": "Maintenance bot with Slack + SMS channels on AWS Lambda",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "sam build && sam deploy",
    "validate": "sam validate"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1029.0",
    "@aws-sdk/client-pinpoint": "^3.1029.0",
    "@aws-sdk/lib-dynamodb": "^3.1029.0",
    "@slack/web-api": "^7.15.0",
    "googleapis": "^171.4.0",
    "openai": "^6.33.0"
  },
  "devDependencies": {
    "vitest": "^4.1.3"
  }
}
